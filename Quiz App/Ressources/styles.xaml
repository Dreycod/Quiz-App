<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
      
            <SolidColorBrush x:Key="Main_Background" Color="#FF1F1F1F"/>
            <SolidColorBrush x:Key="MainColor" Color="DarkGray"/>

    <Style x:Key="LB_Style" TargetType="{x:Type Label}">
        <Setter Property="FontSize" Value="30"/>
        <Setter Property="Foreground" Value="{StaticResource Main_Background}"/>
        <Setter Property="Margin" Value="20,0"/>
    </Style>

    <Style x:Key="PB_Main" TargetType="{x:Type ProgressBar}">
        <Setter Property="Foreground" Value="{StaticResource MainColor}"/>
        <Setter Property="Margin" Value="25,20"/>
    </Style>

    <Style x:Key="Btn_Extra" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource MainColor}"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="Margin" Value="25,0"/>
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="BorderBrush" Value="{StaticResource MainColor}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>